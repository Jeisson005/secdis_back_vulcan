"""Detections app."""

# Django
from django.apps import AppConfig


class DetectionsAppConfig(AppConfig):
    """Detections app config."""
    name = 'hefesto_be.fire.detections'
    verbose_name = 'Detections'
